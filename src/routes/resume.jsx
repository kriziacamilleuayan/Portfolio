import { Link } from "react-router-dom";

const Resume = () => {
  return (
    <>
      <div>resume</div>
      <Link to={`/`}>
        <a>Your Name</a>
      </Link>
    </>
  );
};

export default Resume;
