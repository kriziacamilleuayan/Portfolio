import { Box } from "@mui/system";

const About = () => {
  return (
    <Box
      style={{
        background: "light-gray",
        height: "100vh",
      }}
    >
      about
    </Box>
  );
};

export default About;
